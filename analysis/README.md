# Analysis

F6: false-positive audit and held-out validation against the positive
control. `false_positive_audit.md` is written by a human after
`runbook/07_controls.sh` produces `control-<SCROLL>/control_prediction.tif` —
compare it against the control segment's published ground-truth ink labels
and state plainly why any apparent signal on the target scroll is or isn't
credible (dossier D-07: "Tell us why you are confident that the results you
are getting are real.").

Not written yet — depends on F1 (scroll pick) and F6 (control run).
