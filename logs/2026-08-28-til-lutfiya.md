# TIL — Lutfiya, 2026-08-28

The control run earned its keep twice. First it came back all-zero — which
turned out to be a real registration mismatch (the labeled mesh belongs to
a 2.4 µm volume, not the 9.362 µm one), caught before it could touch the
target. Then, fixed, it came back with legible letterforms and the
ground-truth labels sitting exactly on top of them. Threshold locked at
0.7843. Target inference ran the same day: nine candidates survived the
mechanical criteria, eight of them rendering-boundary artifacts our prereg
already excluded, one a 0.55 mm blob that looks like plain texture at the
control's scale.
